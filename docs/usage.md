# 🛠️ Usage Guide

### **Running the Application**
Our tool provides two primary ways to interact with the AI Docstring engine.

#### **1. Command Line Interface (CLI)**
The `docgen` command allows you to validate files directly from your terminal.

**Basic Validation:**
```bash
docgen myfile.py